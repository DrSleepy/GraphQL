type User {
  displayName: String!
  age: Int!
}

type Query {
  getAllUsers: [User] # Returns list of all users in the database
}

type Mutation {
  createNewUser(displayName: String!, age: Int!): User
}
